
#pragma once

namespace editor
{
    enum class EditorMenuOptions
    {
        NEW,
        SAVE,
        IMPORT_ENTITY,
        EXPORT_ENTITY,
        DUPLICATE,
        REEXPORTENTITIES,
    };

    enum ToolsMenuOptions
    {
        TRANSLATE_TOOL,
        ROTATE_TOOL,
        PATH_TOOL,
        MEASURE_TOOL,
        N_TOOLS
    };
}
