
#pragma once

namespace editor
{
    enum class EditorMenuOptions
    {
        NEW,
        SAVE,
        IMPORT_ENTITY,
        EXPORT_ENTITY,
        EXPORT_ENTITY_COLLECTION,
        DUPLICATE,
        REEXPORTENTITIES,
    };

    enum ToolsMenuOptions
    {
        TRANSLATE_TOOL,
        ROTATE_TOOL,
        PALETTE_TOOL,
        MEASURE_TOOL,
        N_TOOLS
    };
}
